sed -i '/init {/a \        observeSettings()' feature/src/main/java/com/example/feature/calculator/CalculatorViewModel.kt
sed -i '/private fun observeHistory/i \    private fun observeSettings() {\n        viewModelScope.launch {\n            settingsRepository.themeFlow.collect { theme ->\n                _state.update { it.copy(theme = theme) }\n            }\n        }\n        viewModelScope.launch {\n            settingsRepository.orientationLockFlow.collect { locked ->\n                _state.update { it.copy(orientationLock = locked) }\n            }\n        }\n    }\n' feature/src/main/java/com/example/feature/calculator/CalculatorViewModel.kt
